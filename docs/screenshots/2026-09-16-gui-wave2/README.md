# GUI wave 2 — seven pages, grabbed from the exe

Not from a test process: `dist\wing-ui.exe` built at commit `05a90ac`
(`chore(packaging): rebuild the app with the Console page`), 62,384,663 bytes.

```
.\dist\wing-ui.exe --screenshot dist-shots user-files\example-Vu.snap
```

Exit 0, fixed 1280x760 frame (`ui/__main__.py:90-112`). The run also writes
`diff-b.png`, the magnitude-bar-off A/B variant, which is not attached here.
