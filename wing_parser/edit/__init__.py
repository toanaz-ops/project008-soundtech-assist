"""Editing a scene: a journal of patches, and a writer that applies them.

Nothing in this package imports Qt. The desktop application is one
consumer; a future OSC sink is meant to be another, sending the same
patches to a live console instead of to a file.
"""
